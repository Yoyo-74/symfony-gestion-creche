<form method="post" action="{{ path('app_calendar_childs_delete', {'id': calendar_child.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ calendar_child.id) }}">
    <button class="btn">Delete</button>
</form>
