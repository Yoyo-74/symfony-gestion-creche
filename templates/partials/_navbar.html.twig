<nav class="navbar">
    <div class="navbar-brand">
        <a href="{{ path('app_main') }}" class="navbar-logo">Crèche Manager</a>
        <button class="navbar-burger" onclick="toggleMenu()">
            <span></span>
            <span></span>
            <span></span>
        </button>
    </div>
    
    <div class="navbar-menu">
        <div class="navbar-start">
            <a href="{{ path('app_calendar_index') }}" class="navbar-item">
                <i class="fas fa-calendar"></i> Calendrier
            </a>
            {% if is_granted('ROLE_ADMIN') %}
                <a href="{{ path('app_dashboard_admin') }}" class="navbar-item">
                    <i class="fas fa-chart-line"></i> Dashboard Admin
                </a>
            {% endif %}
            {# <a href="{{ path('app_daily_planning') }}" class="navbar-item">
                <i class="fas fa-calendar-day"></i> Planning du jour
            </a> #}
            <a href="{{ path('app_planning_day') }}" class="navbar-item">
                <i class="fas fa-calendar-day"></i> Planning du jour
            </a>
            <a href="{{ path('app_inscription') }}" class="navbar-item">
                <i class="fas fa-child"></i> Inscription
            </a>
            <a href="{{ path('app_users_index') }}" class="navbar-item">
                <i class="fas fa-user"></i> Utilisateurs
            </a>
            <a href="{{ path('app_childs_index') }}" class="navbar-item">
                <i class="fas fa-baby"></i> Enfants
            </a>
        </div>
        
        <div class="navbar-end">
            {% if app.user %}
                <span class="navbar-item">
                    <i class="fas fa-user"></i> {{ app.user.email }}
                </span>
                <a href="{{ path('app_logout') }}" class="navbar-item">
                    <i class="fas fa-sign-out-alt"></i> Déconnexion
                </a>
            {% else %}
                <a href="{{ path('app_login') }}" class="navbar-item">
                    <i class="fas fa-sign-in-alt"></i> Connexion
                </a>
            {% endif %}
        </div>
    </div>
</nav>